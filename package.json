{
  "name": "quest-kit",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:debug": "playwright test --debug",
    "serve": "http-server ./ -a 0.0.0.0 -p 4173 -c-1"
  },
  "devDependencies": {
    "@playwright/test": "^1.43.0",
    "http-server": "^14.1.1"
  }
}
